<div class="row">
  <div class="twelve columns">
          {{alert}}
  </div>
</div>


<div class="row">

{{{sidebar}}}

	<div class="ten columns">

{{{upload_flag}}}

		<h3 class="subheader">{{subheader}}</h3>

		<form method="post" action="" name="{{form_name}}" id="{{form_name}}" data-validate="parsley">
  			<fieldset>

    		<legend>{{legend}}</legend>

          {{#form}}

            {{{.}}}

          {{/form}}

  			</fieldset>

  			<!-- CSRF protection yall -->

				<input type="hidden" name="{{csrf_key}}" value="{{csrf_token}}">

  			<input type="submit" class="button submitter {{submit_class}}" name="{{submit_name}}" value="{{submit_value}}">

		</form>



	</div>

</div>


<div id="modal" class="reveal-modal xlarge">
  <h2>{{modal_head}}</h2>
  <p class="lead">{{modal_subhead}}</p>
  <p>{{modal_text}}</p>
  <a href="{{modal_link}}" class="button alert">Close</a>
  <a class="close-reveal-modal" href="{{modal_link}}">&#215;</a>
</div>

